function digPow(n, p){
    // Break the number down
    n = n.toString().split('').map(number => parseInt(number))
    console.log(n)
    for (let x = 0; x < n.length; x++) {
        
    }
    // The powers increment with each element in n
    
}
digPow(43, 1)